[workspace]
members = [
    "vmbootspec",
    "app",
    "vmrun",
    "kernel",
]

default-members = [
    "vmbootspec",
    "vmrun",
]

# would want
# [profile.dev.package.kernel]
# but that requires nightly
[profile.dev]
opt-level = 1
debug-assertions = true